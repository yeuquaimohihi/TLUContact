<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="TLUContact"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="TLUContact"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".DonviActivity"
            android:exported="true"
            android:label="Danh Bạ Đơn Vị"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".CbnvActivity"
            android:exported="true"
            android:label="Danh Bạ CBNV"
            android:theme="@style/AppTheme" />
        <activity android:name=".AddDonviActivity" />
        <activity android:name=".AddCbnvActivity" />
        <activity android:name=".EditDonviActivity" />
        <activity android:name=".EditCbnvActivity" />


        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".VerificationActivity"
            android:exported="false" />

    </application>

</manifest>